# gbrain — agent governance

> **The one rule here:** Fork (master). Reads/grounding -> prod RW; rehearse risky writes on the staging sidecar. Never let convenience pick the brain.

Author on staging, never hand-edit prod, deploy via **Gate B** (capture/* PR -> main -> deploy).
Canonical governance: `sync-strategy.md` (vps-infra/docs). Vocabulary: `GLOSSARY.md`
(this repo root on a clean repo, else `~/.hermes/GLOSSARY.md`).
